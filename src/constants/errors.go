package constants

// Shared error codes returned in API JSON `code` field.
const (
	InvalidRequestBody = "INVALID_REQUEST_BODY"
	ValidationError    = "VALIDATION_ERROR"
	InternalError      = "INTERNAL_ERROR"
	Unauthorized       = "UNAUTHORIZED"
	Forbidden          = "FORBIDDEN"
)
